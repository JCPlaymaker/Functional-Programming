%!PS
/fib { dup 2 exch lt { dup 1 sub fib exch 2 sub fib add} { pop 1 } ifelse} def

100 100 moveto
0 15 {
    1 add
    dup
    fib
    10 mul
    dup
    dup
    dup
    0 stack rlineto
    0 exch rlineto
    0 exch sub dup rlineto
    10 add 0 rmoveto
} repeat
stroke
